#!/bin/bash
# Simple quality assessment runner for Colab or local shell
# Usage: !quality-assessment

set -e

echo "🚀 Starting Quality Assessment..."

# Install dependencies if needed
pip install boto3 opencv-python pillow numpy python-dotenv

# Run the quality assessment for all folders
python src/quality_assessment.py --all-folders

echo "✅ Quality assessment completed! Results in quality_mapping.json" 